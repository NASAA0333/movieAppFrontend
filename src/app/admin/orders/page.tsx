const Order = () => {
  //   const { data, error } = useQuery("order", fetchOrder);

  //   if (error) {
  //     return <div>Error: {error.message}</div>;
  //   }

  //   if (!data) {
  //     return <div>Loading...</div>;
  //   }

  return (
    <div>
      <h1>Order</h1>
    </div>
  );
};
export default Order;
